def reverse(text):

    return text[::-1]
    # char_list = list(text)
    # reversed_word = ""
    # index = len(char_list) - 1
    # while index >= 0:
    #     reversed_word += char_list[index]
    #     index-=1

    # return reversed_word